interface:
  display_name: "Sendero · Refresh trip"
  short_description: "Recheck weather, events, closures, and routes"
  default_prompt: "Use $sendero-refresh-trip to refresh my itinerary with current weather, events, closures, transport, and reservation information."
